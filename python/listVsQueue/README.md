# Explanation
This Python script compares the execution speed and memory usage of different data structures, lists (list) and queues (deque) when handling add and remove operations.
It also includes utility functions to measure runtime, object memory size, and formatted byte output.